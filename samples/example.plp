begin    P_ROLLBACK := nvl(P_ROLLBACK, false);    createLog(true);    begin         if not P_ROLLBACK then                      createTune(sTuneName                                  ,'INSUR. Точность поиска действующего полиса по серии и номеру'                      ,'0 - полное совпадение серии и номера при поиске действующего полиса, 1 - игнорируются спецсимволы + в серии и номере полиса заменяются латинские символы на идентичные в кириллице'                      ,'NUMBER'                      , 1                      , null);                    &msg('Настройка выполнена!');                    else            &msg('Режим запуска. Откат: ');            deleteTune(sTuneName);                    &msg('Откат завершен');        end if;            end;            createLog(false);end;
